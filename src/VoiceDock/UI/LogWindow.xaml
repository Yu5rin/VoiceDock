<Window x:Class="VoiceDock.UI.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VoiceDock ログ"
        Width="640" Height="480" MinWidth="480" MinHeight="320"
        Style="{StaticResource WindowStyle}"
        WindowStartupLocation="CenterScreen"
        Closed="Window_Closed">
    <Grid Margin="20,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="ログ" FontSize="18" FontWeight="Bold"/>
        <TextBlock Grid.Row="1" Style="{StaticResource SubText}" Margin="0,4,0,10"
                   Text="エラー・動作ログと認識テキストの履歴を表示します（ファイルには 30 日分保存）"/>

        <ListBox Grid.Row="2" x:Name="LogList"
                 FontFamily="Consolas, Yu Gothic UI"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 VirtualizingPanel.IsVirtualizing="True"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,12,0,0">
            <Button Content="ログフォルダを開く" Click="OpenFolder_Click"/>
        </StackPanel>
    </Grid>
</Window>
